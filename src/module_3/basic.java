package module_3;
import java.util.*;

public class basic {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);

        String s = sc.nextLine(); // if i wrote sc.next(); the print only one word not consider the next word

        System.out.println(s + "\t" + s.length());

    }
}
